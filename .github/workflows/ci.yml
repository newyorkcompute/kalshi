name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Cancel in-progress runs for the same branch
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changeset:
    name: Changeset Check
    runs-on: ubuntu-latest
    timeout-minutes: 5
    if: github.event_name == 'pull_request'
    steps:
      - name: Check for skip-changeset label
        id: check-label
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # Fetch current labels from API (not stale event payload)
          LABELS=$(gh api repos/${{ github.repository }}/issues/${{ github.event.pull_request.number }}/labels --jq '.[].name')
          if echo "$LABELS" | grep -q "skip-changeset"; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "✅ skip-changeset label found, skipping check"
          else
            echo "skip=false" >> $GITHUB_OUTPUT
            echo "📋 No skip-changeset label, will run check"
          fi

      - name: Checkout
        if: steps.check-label.outputs.skip != 'true'
        uses: actions/checkout@v4
        with:
          # Only need enough history to compare against main
          fetch-depth: 0

      - name: Setup Node.js
        if: steps.check-label.outputs.skip != 'true'
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        if: steps.check-label.outputs.skip != 'true'
        run: npm ci

      - name: Check for changeset
        if: steps.check-label.outputs.skip != 'true'
        run: |
          echo "Checking for changeset..."
          npx changeset status --since=origin/main

  # Main CI job - runs typecheck, lint, test, build
  ci:
    name: CI
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      # Determine if we should use affected (PRs) or run-many (main)
      IS_PR: ${{ github.event_name == 'pull_request' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # Fetch all history for nx affected
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      # Cache NX task results for faster subsequent runs
      - name: Cache NX
        uses: actions/cache@v4
        with:
          path: .nx/cache
          # Use lock file hash (stable) instead of commit SHA (unique per commit)
          key: nx-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            nx-${{ runner.os }}-

      # Set NX_BASE for affected commands
      - name: Derive SHAs for nx affected
        id: set-shas
        uses: nrwl/nx-set-shas@v4

      - name: Typecheck
        run: |
          if [ "$IS_PR" == "true" ]; then
            echo "🔍 Running typecheck on affected projects..."
            npx nx affected -t typecheck --base=${{ steps.set-shas.outputs.base }} --head=${{ steps.set-shas.outputs.head }}
          else
            echo "🔍 Running typecheck on all projects..."
            npx nx run-many -t typecheck
          fi

      - name: Lint
        run: |
          if [ "$IS_PR" == "true" ]; then
            echo "🔍 Running lint on affected projects..."
            npx nx affected -t lint --base=${{ steps.set-shas.outputs.base }} --head=${{ steps.set-shas.outputs.head }}
          else
            echo "🔍 Running lint on all projects..."
            npx nx run-many -t lint
          fi

      - name: Test with Coverage
        run: |
          if [ "$IS_PR" == "true" ]; then
            echo "🧪 Running tests on affected projects..."
            npx nx affected -t test:coverage --base=${{ steps.set-shas.outputs.base }} --head=${{ steps.set-shas.outputs.head }}
          else
            echo "🧪 Running tests on all projects..."
            npx nx run-many -t test:coverage
          fi

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          # Use directory instead of explicit files - handles missing coverage gracefully
          directory: ./packages
          flags: unittests
          fail_ci_if_error: false
          verbose: true

      - name: Build
        run: |
          if [ "$IS_PR" == "true" ]; then
            echo "🏗️ Building affected projects..."
            npx nx affected -t build --base=${{ steps.set-shas.outputs.base }} --head=${{ steps.set-shas.outputs.head }}
          else
            echo "🏗️ Building all projects..."
            npx nx run-many -t build
          fi
